services:
  kali-api:
    build: .
    container_name: kali-api
    ports:
      - "192.168.50.21:3000:3000"
    environment:
      - API_PORT=3000
      - COMMAND_TIMEOUT=600
      - DEBUG_MODE=0
    cap_add:
      - NET_RAW
      - NET_ADMIN
    restart: unless-stopped
