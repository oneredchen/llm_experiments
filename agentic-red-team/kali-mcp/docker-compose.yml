services:
  kali-mcp:
    build: .
    container_name: kali-mcp
    ports:
      - "3000:3000"
    environment:
      - API_PORT=3000
      - COMMAND_TIMEOUT=180
      - DEBUG_MODE=0
    restart: unless-stopped
